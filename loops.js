for (let rep = 0; rep < 10; rep++) {
  console.log(rep, "repppssss");
}

const vings = ["vihang", "yash", "Peter"];

//accesses all the indexes
for (let i = 0; i < vings.length; i++) {
  console.log(vings[i], typeof vings[i]);
}
